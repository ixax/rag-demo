qdrant-client==1.9.1
PyYAML==6.0.2
httpx==0.27.0
environs==15.0.1
# already a transitive dep of qdrant-client -- pinned explicitly since
# ingest.py imports it directly now (_common.config's config.yml schema
# validation, shared with mcp-server/reranker).
pydantic==2.13.4
